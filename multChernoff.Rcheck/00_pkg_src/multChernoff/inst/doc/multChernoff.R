## ----include = FALSE----------------------------------------------------------
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)

## -----------------------------------------------------------------------------
library(multChernoff)

## -----------------------------------------------------------------------------
tailProbBound(x = 20, k = 7, n = 50)

## -----------------------------------------------------------------------------
tailProbBound(x = 12, k = c(3, 4), n = c(12, 20))

## -----------------------------------------------------------------------------
criticalValue(k = 10, n = 40, p = 0.05)

## -----------------------------------------------------------------------------
qchisq(0.95, df = 9)

## -----------------------------------------------------------------------------
mgfBound(k = 10, n = 20, lambda = c(0, 0.25, 0.5))

